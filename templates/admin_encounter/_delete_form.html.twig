<form method="post" action="{{ path('app_admin_encounter_delete', {'id': encounter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encounter.id) }}">
    <button class="btn">Delete</button>
</form>
