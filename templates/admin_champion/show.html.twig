{% extends 'base.html.twig' %}

{% block title %}Champion{% endblock %}
{% block pageTitle %}Champion{% endblock %}

{% block bodyContent %}
    <h1>Champion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champion.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ champion.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ champion.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_champion_index') }}">back to list</a>

    <a href="{{ path('app_admin_champion_edit', {'id': champion.id}) }}">edit</a>

    {{ include('admin_champion/_delete_form.html.twig') }}
{% endblock %}
