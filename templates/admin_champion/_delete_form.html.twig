<form method="post" action="{{ path('app_admin_champion_delete', {'id': champion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ champion.id) }}">
    <button class="btn">Delete</button>
</form>
