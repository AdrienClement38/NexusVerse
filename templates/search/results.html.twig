{% extends 'base.html.twig' %}

{% block title %}Results{% endblock %}

{% block pageTitle %}Results{% endblock %}

{% block bodyContent %}
    {% if typedResults is empty %}
        <p>No results found.</p>
    {% else %}
        {% for type, results in typedResults %}

            {% if type == 'App\\Entity\\Player' %}
                <h2> Player(s) :</h2>
                {% for result in results %}
                    <div>
                        <h3>{{ result.pseudo }}</h3>
                        <p>FirstName: {{ result.firstName }}</p>
                        <p>LastName: {{ result.lastName }}</p>
                    </div>
                {% endfor %}


            {% elseif type == 'App\\Entity\\Tournament' %}
                <h2> Tournament(s) :</h2>
                {% for result in results %}
                    <a href={{ path('app_tournament_show', {'id': result.id})}}>
                        <div>
                            <h3>{{ result.name }}</h3>
                            <p>Start: {{ result.startDate|date("d/m/Y H:i") }}</p>
                            <p>End: {{ result.endDate|date("d/m/Y H:i") }}</p>
                        </div>
                    </a>
                {% endfor %}

            {% elseif type == 'App\\Entity\\League' %}
                <h2> League(s) :</h2>
                {% for result in results %}
                    <a href={{ path('app_league_show', {'id': result.id})}}>
                        <div>
                            <h3>{{ result.name }}</h3>
                        </div>
                    </a>
                {% endfor %}

            {% elseif type == 'App\\Entity\\Team' %}
                <h2> Team(s) :</h2>
                {% for result in results %}
                    <a href={{ path('app_team_show', {'id': result.id})}}>
                        <div>
                            <h3>Name: {{ result.name }}</h3>
                            <p>Country: {{ result.country }}</p>
                        </div>
                    </a>
                {% endfor %}

            {% elseif type == 'App\\Entity\\Champion' %}
                <h2> Champion(s) :</h2>
                {% for result in results %}
                    <div>
                        <h3>Name: {{ result.name }}</h3>
                        <p>Description: {{ result.description }}</p>
                    </div>
                {% endfor %}

            {% endif %}
        {% endfor %}
    {% endif %}
{% endblock %}
